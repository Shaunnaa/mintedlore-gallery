"use client";

export function HolderBadge({
  collectionAddress,
  communitySlug,
  vipThreshold,
}: {
  collectionAddress: string;
  communitySlug: string;
  vipThreshold: number;
}) {
  // Removed from UI by request
  return null;
}
